const CONFIG_KEY = 'cypress-playback::config';
const ENV_MODE = 'PLAYBACK_MODE';
/**
 * The current version of the serialized requests and responses.
 */
const SERIALIZE_VERSION = 1;

module.exports = {
  CONFIG_KEY,
  ENV_MODE,
  SERIALIZE_VERSION
};
